const menu=document.querySelector('.menu'),links=document.querySelector('.links');if(menu&&links)menu.addEventListener('click',()=>{const o=links.classList.toggle('open');menu.setAttribute('aria-expanded',o)});
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const progress=document.querySelector('.progress');addEventListener('scroll',()=>{if(progress){const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h?scrollY/h*100:0)+'%'}},{passive:true});
document.querySelectorAll('.edition-head').forEach(b=>b.addEventListener('click',()=>{const card=b.closest('.edition-card');document.querySelectorAll('.edition-card').forEach(c=>{if(c!==card){c.classList.remove('open');c.querySelector('.edition-head')?.setAttribute('aria-expanded','false')}});const o=card.classList.toggle('open');b.setAttribute('aria-expanded',String(o))}));
const lq=document.querySelector('[data-lens-question]'),lt=document.querySelector('[data-lens-title]'),lc=document.querySelector('[data-lens-copy]');document.querySelectorAll('.lens-btn').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.lens-btn').forEach(x=>x.classList.remove('active'));b.classList.add('active');if(lq)lq.textContent='“'+b.dataset.question+'”';if(lt)lt.textContent=b.dataset.title;if(lc)lc.textContent=b.dataset.copy}));
const fh=document.querySelector('[data-finding-heading]'),ft=document.querySelector('[data-finding-text]');document.querySelectorAll('.finding-btn').forEach((b,i)=>b.addEventListener('click',()=>{document.querySelectorAll('.finding-btn').forEach(x=>x.classList.remove('active'));b.classList.add('active');if(fh)fh.textContent=b.dataset.findingTitle;if(ft)ft.textContent=b.dataset.findingCopy;const k=document.querySelector('.finding-panel .kicker');if(k)k.textContent='Finding '+String(i+1).padStart(2,'0')}));
const applicationForm=document.querySelector('#application-form');if(applicationForm){const status=document.querySelector('#form-status'),success=document.querySelector('#application-success'),submit=applicationForm.querySelector('button[type="submit"]');applicationForm.addEventListener('submit',async e=>{e.preventDefault();status.classList.remove('error');if(!applicationForm.checkValidity()){applicationForm.reportValidity();return}submit.disabled=true;status.textContent='Submitting…';try{const response=await fetch('/.netlify/functions/apply',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(applicationForm).entries()))});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'We could not submit your application. Please try again.');applicationForm.hidden=true;success.hidden=false;success.scrollIntoView({behavior:'smooth',block:'center'})}catch(err){status.textContent=err.message||'We could not submit your application. Please try again.';status.classList.add('error')}finally{submit.disabled=false}})}