const menu=document.querySelector('.menu'),links=document.querySelector('.links');if(menu&&links)menu.addEventListener('click',()=>{const o=links.classList.toggle('open');menu.setAttribute('aria-expanded',o)});
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach((el,i)=>{el.style.transitionDelay=Math.min(i%4,3)*55+'ms';io.observe(el)});
const progress=document.querySelector('.progress');addEventListener('scroll',()=>{if(progress){const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h?scrollY/h*100:0)+'%'}} ,{passive:true});
document.querySelectorAll('.edition button').forEach(b=>b.addEventListener('click',()=>{const e=b.closest('.edition'),o=e.classList.toggle('open');b.setAttribute('aria-expanded',o)}));
const steps=[...document.querySelectorAll('.think-step')],decision=document.querySelector('.decision'),count=document.querySelector('[data-count]'),title=document.querySelector('[data-title]'),copy=document.querySelector('[data-copy]');steps.forEach((s,i)=>s.addEventListener('click',()=>{steps.forEach(x=>x.classList.remove('active'));s.classList.add('active');if(decision){decision.classList.add('changing');setTimeout(()=>{decision.textContent='“'+s.dataset.question+'”';decision.classList.remove('changing')},160)}if(count)count.textContent=String(i+1).padStart(2,'0');if(title)title.textContent=s.dataset.title;if(copy)copy.textContent=s.dataset.copy}));
