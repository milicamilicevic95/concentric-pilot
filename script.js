const q=(s,c=document)=>c.querySelector(s), qa=(s,c=document)=>[...c.querySelectorAll(s)];
const nav=q('.site-nav'), toggle=q('.mobile-toggle');
if(toggle){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));toggle.textContent=open?'Close':'Menu';});}
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.08});qa('.reveal').forEach(el=>io.observe(el));
qa('[data-tabs]').forEach(group=>{const buttons=qa('[data-tab]',group);const panels=qa('[data-panel]',group);buttons.forEach(btn=>btn.addEventListener('click',()=>{buttons.forEach(b=>b.classList.toggle('active',b===btn));panels.forEach(p=>p.classList.toggle('active',p.dataset.panel===btn.dataset.tab));}));});
qa('[data-prompt-widget]').forEach(widget=>{const prompts=JSON.parse(widget.dataset.prompts||'[]');const out=q('[data-prompt-output]',widget);const dots=q('[data-prompt-dots]',widget);const items=qa('[data-prompt-item]',widget);let idx=0;const render=()=>{if(out)out.textContent=prompts[idx]||'';if(dots){dots.innerHTML='';prompts.forEach((_,i)=>{const b=document.createElement('button');b.type='button';b.className=i===idx?'active':'';b.setAttribute('aria-label',`Show prompt ${i+1}`);b.addEventListener('click',()=>{idx=i;render()});dots.appendChild(b);});}items.forEach((it,i)=>it.classList.toggle('active',i===idx));};items.forEach((it,i)=>it.addEventListener('click',()=>{idx=i;render()}));render();});
