(()=>{const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
const toggle=q('.mobile-toggle'),links=q('.nav-links');toggle?.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));links?.classList.toggle('open',!open)});qa('.nav-links a').forEach(a=>a.addEventListener('click',()=>{links?.classList.remove('open');toggle?.setAttribute('aria-expanded','false')}));
const progress=q('.progress');const onScroll=()=>{if(!progress)return;const d=document.documentElement;progress.style.width=((d.scrollTop/(d.scrollHeight-d.clientHeight))*100||0)+'%'};addEventListener('scroll',onScroll,{passive:true});onScroll();
const reveals=qa('.reveal');if(reduced)reveals.forEach(x=>x.classList.add('in'));else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.12});reveals.forEach(x=>io.observe(x))}
const thinkData={question:['01','Ask questions.','Surface what may be underneath the decision, rather than simply react to the version first presented.'],challenge:['02','Challenge assumptions.','Question what is being treated as fixed, inevitable or already decided.'],perspective:['03','Introduce perspective.','Bring different professional contexts, experiences and ways of seeing the same problem.'],stress:['04','Stress-test the thinking.','Test a plan, decision or interpretation before it is acted on.']};const thinkButtons=qa('[data-think]'),thinkBox=q('.thinking-copy'),thinkNo=q('[data-think-number]'),thinkTitle=q('[data-think-title]'),thinkBody=q('[data-think-body]');function setThink(btn){const d=thinkData[btn.dataset.think];if(!d)return;thinkButtons.forEach(b=>{b.classList.toggle('active',b===btn);b.setAttribute('aria-selected',String(b===btn))});thinkBox?.classList.add('is-changing');setTimeout(()=>{if(thinkNo)thinkNo.textContent=d[0];if(thinkTitle)thinkTitle.textContent=d[1];if(thinkBody)thinkBody.textContent=d[2];thinkBox?.classList.remove('is-changing')},120)}thinkButtons.forEach((b,i)=>{b.addEventListener('click',()=>setThink(b));b.addEventListener('mouseenter',()=>setThink(b));b.addEventListener('keydown',e=>{if(!['ArrowUp','ArrowDown'].includes(e.key))return;e.preventDefault();const n=(i+(e.key==='ArrowDown'?1:-1)+thinkButtons.length)%thinkButtons.length;thinkButtons[n].focus();setThink(thinkButtons[n])})});
qa('.edition-row').forEach(row=>{const activate=()=>qa('.edition-row').forEach(r=>r.classList.toggle('active',r===row));row.addEventListener('mouseenter',activate);row.addEventListener('focus',activate);row.addEventListener('click',activate)});
const evidence={direction:'Questioning whether the current professional path was still the right one.',isolation:'Managing professional difficulty largely alone.',access:'Lacking some of the relationships, introductions, advocacy or opportunities needed despite having professional contacts.',recalibration:'Reconsidering work after burnout, depletion or an unsustainable role.'};const eBtns=qa('[data-evidence]'),eDetail=q('[data-evidence-detail]');function setEvidence(btn){eBtns.forEach(b=>b.classList.toggle('active',b===btn));if(!eDetail)return;eDetail.classList.add('is-changing');setTimeout(()=>{eDetail.textContent=evidence[btn.dataset.evidence]||'';eDetail.classList.remove('is-changing')},120)}eBtns.forEach(b=>{b.addEventListener('click',()=>setEvidence(b));b.addEventListener('mouseenter',()=>setEvidence(b));b.addEventListener('focus',()=>setEvidence(b))});
const form=q('[data-application-form]'),status=q('[data-form-status]');if(form)form.addEventListener('submit',async e=>{e.preventDefault();const btn=q('button[type=submit]',form);if(status){status.textContent='Submitting…';status.className='form-status'}if(btn)btn.disabled=true;try{const payload=Object.fromEntries(new FormData(form).entries());const r=await fetch('/.netlify/functions/apply',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||'Submission failed');if(status){status.className='form-status success';status.textContent='Thank you. Your application has been submitted.'}form.reset()}catch(err){if(status){status.className='form-status error';status.textContent='We could not submit your application. Please try again.'}}finally{if(btn)btn.disabled=false}});
})();
